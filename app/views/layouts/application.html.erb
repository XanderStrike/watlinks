<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Watlinks" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Watlinks" %>">
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    <%= yield(:head) %>
  </head>
  <body class="<%= controller_name %> <%= action_name %>">

    <% if user_signed_in? %>
      <div class="container">
        <div class="row-fluid">
          <div class="span12">
            <div class="page-header">
              <h1>
                Watlinks <small>because nolinks is ugly</small>
              </h1>
            </div>
          </div>
        </div>
        <div class="row-fluid">
          <div class="span2">
            <ul class="nav nav-list">
              <li <%= isactive("/").html_safe %>><a href="/">Home</a></li>
              <li class="nav-header">Posts</li>

              <li <%= isactive("/links") %> ><a href="/links">Links</a></li>
              <% if request.fullpath.include?('links') %>
                <ul class="nav nav-list">
                  <% categories.each do |c| %>
                    <li <%= isactive("/links/#{c}") %> ><%= link_to c.capitalize, "/links/#{c}" %></li>
                  <% end %>
                </ul>
              <% end %>

              <li <%= isactive("/requests") %> ><a href="/requests">Requests</a></li>
              <% if request.fullpath.include?('requests') %>
                <ul class="nav nav-list">
                  <% categories.each do |c| %>
                    <li <%= isactive("/requests/#{c}") %> ><%= link_to c.capitalize, "/requests/#{c}" %></li>
                  <% end %>
                </ul>
              <% end %>

              <li <%= isactive("/forums") %> ><a href="/forums">Forums</a></li>
              <% if request.fullpath.include?('forums') %>
                <ul class="nav nav-list">
                  <% categories.each do |c| %>
                    <li <%= isactive("/forums/#{c}") %> ><%= link_to c.capitalize, "/forums/#{c}" %></li>
                  <% end %>
                </ul>
              <% end %>

              <li class="nav-header">Account</li>
              <li <%= isactive("/users/#{current_user.id}") %>><%= link_to "My Profile", current_user %></li>
              <li <%= isactive("") %> ><a href="#">Send Invite (0)</a></li>
              <li class="nav-header">Read This</li>
              <li <%= isactive("/rules") %> ><a href="/rules">Rules</a></li>
              <li <%= isactive("/faq") %> ><a href="/faq">FAQ</a></li>
              <li <%= isactive("/blog") %> ><a href="/blog">Dev Blog</a></li>
              <li class="nav-header"></li>
              <li><%= link_to 'Logout', destroy_user_session_path, :method=>'delete' %></li>
            </ul>
          </div>
          <div class="span9">
            <%= yield %>
          <footer>
            <br><br><br>
          </footer>
          </div>
        </div>
      </div>


    <% else %>
<!-- You'll find no secrets here. -->
<div class="navbar navbar-fixed-top">
<nav class="navbar-inner">
<div class="container">
<%= render 'layouts/login' %>
</div>
</nav>
</div>
<div id="main" role="main">
<div class="container">
<%= render 'layouts/messages' %>
<%= yield %>

</div>
</div>

    <% end %>
    
  </body>
</html>
